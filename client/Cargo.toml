[package]
name = "client"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.32", features = ["derive"] }
shared = { path = "../shared" }
uuid = { version = "1.16.0", features = ["v4"] }
